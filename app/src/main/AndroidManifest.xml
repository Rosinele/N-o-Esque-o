<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marcarconsulta_login">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.apps.photos.permission.GOOGLE_PHOTOS" />

    <application
        android:allowBackup="true"
        android:background="#8873B4"
        android:icon="@mipmap/logo"
        android:label="Não+Esqueço"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListaNoDia"></activity>
        <activity android:name=".Pre" />
        <activity android:name=".CadastrarSessao" />
        <activity android:name=".ListaPos" />
        <activity android:name=".ListaPre" />
        <activity android:name=".ResetSenha" />
        <activity android:name=".Cadastro" />
        <activity android:name=".Perfil" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>